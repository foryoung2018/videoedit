<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <solid
        android:color="@color/white" />
    <!-- 边缘线条颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/white" />
    <!-- 圆角的幅度 -->
    <corners
        android:topLeftRadius="@dimen/t4dp"
        android:topRightRadius="@dimen/t4dp"
        android:bottomLeftRadius="@dimen/t4dp"
        android:bottomRightRadius="@dimen/t4dp" />

</shape>