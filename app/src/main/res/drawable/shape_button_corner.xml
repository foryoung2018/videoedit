<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 内部颜色 -->
    <stroke
        android:width="1dp"
        android:color="@color/shoot_button_color" />
    <!-- 圆角的幅度 -->
    <corners
        android:topLeftRadius="@dimen/t22dp"
        android:topRightRadius="@dimen/t22dp"
        android:bottomLeftRadius="@dimen/t22dp"
        android:bottomRightRadius="@dimen/t22dp" />

</shape>